const initialState = {
  name: '',
  mode: 'javascript',
  level: 'beginner',
  lessons: [
    { id: 'l1', example: '', exercise: '' }
  ]
}

export default (state = initialState, action) => {
  return state
}
